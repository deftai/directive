{
  "xBRIEFInfo": {
    "version": "0.8",
    "description": "Scope xBRIEF ingested from GitHub issue #3903",
    "updated": "2026-08-28T23:49:57Z"
  },
  "plan": {
    "title": "bug(scm,release): release Step 3 fails with an empty-stderr exit 1 once the open-issue inventory exceeds 1 MB (ENOBUFS in scm call)",
    "status": "running",
    "narratives": {
      "Description": "bug(scm,release): release Step 3 fails with an empty-stderr exit 1 once the open-issue inventory exceeds 1 MB (ENOBUFS in scm call)",
      "Origin": "Ingested from https://github.com/deftai/directive/issues/3903",
      "Overview": "## Summary\n\n`task release` cannot cut a release on this repo. Step 3 (pre-flight xBRIEF lifecycle sync) fetches the complete open-issue inventory through `scm/call.ts::call`, whose `spawnSync` omits `maxBuffer`. Node defaults to 1 MB. This repo's inventory is ~4.1 MB, so the capture aborts with `ENOBUFS`: `status === null`, truncated stdout, empty stderr.\n\n`call()` reports `returncode: result.status ?? 1` and never surfaces `result.error.message`, so the operator sees an exit 1 with no reason and a hint that points at the wrong thing:\n\n```\nopen-issue inventory fetch failed: gh api failed: endpoint='repos/deftai/directive/issues?state=open&per_page=100' exit=1 stderr=''; hint: verify gh auth and core REST quota; open-issue inventory must be complete\n[3/13] Pre-flight vBRIEF lifecycle sync... FAIL (failed to fetch issue states from gh)\n```\n\nAuth was healthy and `core.remaining` was 4985 at the time of failure. The same `ghx api --paginate --slurp` call succeeds from a shell and under a plain `spawnSync` that passes the ceiling.\n\n## Reproduction\n\n```\nnode -e \"\nconst {spawnSync}=require('node:child_process');\nconst r=spawnSync('ghx',['api','--paginate','--slurp','repos/deftai/directive/issues?state=open&per_page=100'],{encoding:'utf8',timeout:120000,env:process.env,stdio:['pipe','pipe','pipe']});\nconsole.log(r.status, r.error && r.error.code, (r.stderr||'').length, (r.stdout||'').length);\n\"\n```\n\nObserved: `null ENOBUFS 0 1112000`. With `maxBuffer: 64*1024*1024` added: `0 undefined 0 4158002`.\n\n## Root cause\n\n- `packages/core/src/scm/call.ts` -- `spawnSync` passes no `maxBuffer`.\n- `packages/core/src/subprocess/max-buffer.ts` already documents the rule this violates: every capture site that may produce a large response MUST pass `SUBPROCESS_MAX_BUFFER`, and MUST surface `result.error.message` when stderr is empty so an overflow is never silent again (#1867). `runGhApi` in `scm/gh-rest.ts` does both. `call()` does neither.\n- `packages/core/src/release/issue-state-fetch.ts::scmCallToRunGhApi` routes the #3752 paginated inventory through `call()`, so the one-subprocess-complete-inventory design inherits a 1 MB ceiling.\n\nThe trigger is repository size, not configuration: the failure arrives silently the first time a repo's open-issue list crosses 1 MB, and is permanent from then on.\n\n## Impact\n\nBlocks every `task release` cut on deftai/directive at Step 3, before any mutation. The only current escape is `--allow-vbrief-drift`, which skips the lifecycle gate the step exists to enforce -- trading a release blocker for an unchecked lifecycle invariant.\n\n## Acceptance criteria\n\n1. `call()` passes `SUBPROCESS_MAX_BUFFER` to `spawnSync`.\n2. `call()` surfaces `result.error.message` when `status === null` and stderr is empty, so ENOBUFS and timeout kills never report as a bare exit 1 with no reason.\n3. Regression test: a `call()` capture whose stdout exceeds 1 MB succeeds, and a forced spawn-level failure reports a non-empty reason.\n4. `task release -- <version> --dry-run` Step 3 completes against this repo (open-issue inventory over 1 MB).\n5. Remaining `spawnSync` / `execFileSync` capture sites that can return large payloads are audited; each either passes the ceiling or records why it cannot.\n\nRefs #1867, #3752, #3767.",
      "Labels": "bug, blocks-release-tag"
    },
    "items": [
      {
        "title": "`call()` passes `SUBPROCESS_MAX_BUFFER` to `spawnSync`.",
        "status": "proposed"
      },
      {
        "title": "`call()` surfaces `result.error.message` when `status === null` and stderr is empty, so ENOBUFS and timeout kills never report as a bare exit 1 with no reason.",
        "status": "proposed"
      },
      {
        "title": "Regression test: a `call()` capture whose stdout exceeds 1 MB succeeds, and a forced spawn-level failure reports a non-empty reason.",
        "status": "proposed"
      },
      {
        "title": "`task release -- <version> --dry-run` Step 3 completes against this repo (open-issue inventory over 1 MB).",
        "status": "proposed"
      },
      {
        "title": "Remaining `spawnSync` / `execFileSync` capture sites that can return large payloads are audited; each either passes the ceiling or records why it cannot.",
        "status": "proposed"
      }
    ],
    "tags": [
      "bug",
      "blocks-release-tag"
    ],
    "references": [
      {
        "uri": "https://github.com/deftai/directive/issues/3903",
        "type": "x-xbrief/github-issue",
        "title": "Issue #3903: bug(scm,release): release Step 3 fails with an empty-stderr exit 1 once the open-issue inventory exceeds 1 MB (ENOBUFS in scm call)"
      },
      {
        "uri": "https://github.com/deftai/directive/issues/1867",
        "type": "x-xbrief/refs",
        "title": "Issue #1867"
      }
    ],
    "metadata": {
      "literal_acceptance_commands": [],
      "literal_acceptance_rejected": [
        {
          "command": "node -e \"",
          "reason": "first token \"node\" is not in the literal-AC allowlist",
          "sourceSpan": "fence@L18"
        },
        {
          "command": "const {spawnSync}=require('node:child_process');",
          "reason": "shell metacharacter \"{\" is not allowed in literal AC commands",
          "sourceSpan": "fence@L18"
        },
        {
          "command": "const r=spawnSync('ghx',['api','--paginate','--slurp','repos/deftai/directive/issues?state=open&per_page=100'],{encoding:'utf8',timeout:120000,env:process.env,stdio:['pipe','pipe','pipe']});",
          "reason": "shell metacharacter \"(\" is not allowed in literal AC commands",
          "sourceSpan": "fence@L18"
        },
        {
          "command": "console.log(r.status, r.error && r.error.code, (r.stderr||'').length, (r.stdout||'').length);",
          "reason": "shell metacharacter \"(\" is not allowed in literal AC commands",
          "sourceSpan": "fence@L18"
        },
        {
          "command": "task release -- <version> --dry-run",
          "reason": "inline backtick in prose is a mention, not a stated acceptance command (#3721)",
          "sourceSpan": "inline@L45"
        }
      ],
      "swarm": {},
      "intended_placement": {
        "schema": "deft.scope.intended_placement.v1",
        "files": [],
        "module_boundary": ""
      }
    },
    "acceptance": {
      "commands": [],
      "none_stated": true,
      "source_rung": "derived",
      "derived_reason": "derived 5 independently testable clauses from the task statement before product edit (#3323)",
      "clauses": [
        {
          "id": 1,
          "text": "`call()` passes `SUBPROCESS_MAX_BUFFER` to `spawnSync`.",
          "artifact_path": null,
          "ambiguous": false,
          "provenance": "statement"
        },
        {
          "id": 2,
          "text": "`call()` surfaces `result.error.message` when `status === null` and stderr is empty, so ENOBUFS and timeout kills never report as a bare exit 1 with no reason.",
          "artifact_path": null,
          "ambiguous": false,
          "provenance": "statement"
        },
        {
          "id": 3,
          "text": "Regression test: a `call()` capture whose stdout exceeds 1 MB succeeds, and a forced spawn-level failure reports a non-empty reason.",
          "artifact_path": null,
          "ambiguous": false,
          "provenance": "statement"
        },
        {
          "id": 4,
          "text": "`task release -- <version> --dry-run` Step 3 completes against this repo (open-issue inventory over 1 MB).",
          "artifact_path": null,
          "ambiguous": false,
          "provenance": "statement"
        },
        {
          "id": 5,
          "text": "Remaining `spawnSync` / `execFileSync` capture sites that can return large payloads are audited; each either passes the ceiling or records why it cannot.",
          "artifact_path": null,
          "ambiguous": false,
          "provenance": "statement"
        }
      ],
      "ambiguity_attestation": "none_found"
    },
    "updated": "2026-08-28T23:49:57Z"
  }
}
