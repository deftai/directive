/** Exit codes — mirrors scripts/pr_check_protected_issues.py. */
export const EXIT_OK = 0;
export const EXIT_PROTECTED_LINKED = 1;
export const EXIT_EXTERNAL_ERROR = 2;

/** gh subprocess timeout in seconds (matches Python oracle). */
export const GH_TIMEOUT_S = 30;
