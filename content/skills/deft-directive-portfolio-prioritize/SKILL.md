---
name: deft-directive-portfolio-prioritize
description: >-
  Pre-promotion portfolio prioritization from the local github-issue cache:
  cluster competing RFCs/issues, name conflicts/supersession, and emit a
  priority brief (shortlist + park list) under propose-not-apply. Use when the
  operator asks for a portfolio prioritization pass, priority brief, or
  shortlist/park over a backlog slice. Do NOT trigger on "what's next", triage
  hygiene, per-issue accept, or post-promotion capacity buckets.
triggers:
  - portfolio prioritization
  - priority brief
  - portfolio pass
  - prioritize portfolio
  - shortlist and park
  - RFC portfolio
  - park list
metadata:
  clawdbot:
    requires:
      bins: ["gh"]
---
<!-- AUTO-GENERATED by task packs:render -- DO NOT EDIT MANUALLY -->
<!-- Purpose: rendered skill -->
<!-- Source of truth: packs/skills/skills-pack-0.1.json -->
<!-- Regenerate with: task packs:render -->
<!-- Edit the source, not this file. Slice instead of loading every SKILL.md: task packs:slice skills by-trigger --trigger <kw> (or list) -->

# Deft Directive Portfolio Prioritize (#3198)

Pre-promotion **portfolio prioritization** from the local github-issue cache: cluster competing RFCs/issues, name conflicts/supersession, emit a **priority brief** (shortlist + park list). **Propose-not-apply** (#3179) — no SCM mutations from this pass.

Legend (from RFC2119): !=MUST, ~=SHOULD, ≉=SHOULD NOT, ⊗=MUST NOT, ?=MAY.

Sits **between** automated label-mirror triage (#1423 / #3197 A-side) and post-promotion ranking (#1419 / #1511) / buildable `triage:queue`. Parent tracker context: #886. Decision dispose: #1396 + plan-sequence.

## When to Use

- Operator asks for a **portfolio prioritization** pass, **priority brief**, shortlist+park over a backlog slice
- Large multi-area open backlog (labels / RFC families / patterns) where flat queue re-litigates essays
- After classify dry-run partitions exist and a human wants **portfolio** compare, not single-issue accept

## Do NOT Use

- ⊗ "What's next?" / triage hygiene / build a cohort → `deft-directive-triage` + `task triage:queue`
- ⊗ Per-issue accept/reject/defer/ingest → `deft-directive-refinement` / `task triage:accept`
- ⊗ Post-promotion capacity buckets (#1419 / #1511) or review-cycle owner briefs (#1590)
- ⊗ Unattended auto-promote to plan-sequence or mirror label apply

## Inputs

- ! Local cache: `.deft-cache/github-issue/` (bodies available) — probe freshness first
- ? Optional classify dry-run / plan JSON — use as a **filter** only (e.g. focus `no_match`, separate escalate interrupt queue), **not** as the ranker
- ? Scope hint from operator (label family, title family, author, parent epic)

## Phase 0 — Cache freshness

1. ! Run `task verify:cache-fresh`. Exit 0 → proceed. Exit 1 → refresh (`task cache:fetch-all -- --source github-issue --repo OWNER/NAME` or printed remediation). Exit 2 → `task triage:bootstrap` first.
2. ⊗ Cluster or rank against a stale cache without saying so — state freshness limits in the brief.

## Phase 1 — Scope + filter

1. ! Agree a **pass scope** (label prefix, theme, open-only, max N) with the operator when not already given.
2. ! Load candidates from **local cache** (bodies). Prefer cache over live list walks for clustering.
3. ~ Optional: run/read `task triage:classify` dry-run partitions. Treat escalate/defer/hold as **interrupt / non-portfolio** rows — do not rank them against park-list RFCs.
4. ⊗ Use classify disposition as the sole ranking order.

## Phase 2 — Cluster

1. ! Build **theme / label / title-family** clusters and **RFC families**.
2. ! Build a **conflict / supersession matrix** per family (overlap, pack cake, competing roots).
3. ! Epistemic gates for every cited issue:
   - ! Verify the issue **exists** (live `gh api repos/<o>/<r>/issues/<N>` REST, or fresh cache hit).
   - ! Read the **body** when claiming scope, dependency, or pack membership.
   - ! Read **comments** when claiming decided, deferred, superseded, or closed-as-dup.
   - ! Report **open/closed** accurately.
   - ⊗ Title-only supersession ("#A supersedes #B" from titles alone).
4. ~ Prefer REST over GraphQL `gh issue view --json` under multi-agent GraphQL pressure (#954).

## Phase 3 — Shortlist + park

1. ! Produce an ordered **shortlist** (deep dive / promote candidates) with one-line why each.
2. ! Produce a **park list** (classes + examples + park reason) — park-by-theme is OK; formal supersession needs Phase 2 proof.
3. ! Live-verify (or fresh-cache verify) every shortlist number and representative park examples before publish.
4. ⊗ Auto-apply shortlist as GitHub labels, triage decisions, or plan-sequence without operator dispose.

## Phase 4 — Emit brief + dispose checklist

1. ! Emit the priority brief using [`references/priority-brief-template.md`](references/priority-brief-template.md).
2. ! Brief MUST include: clusters, conflict/supersession matrix, shortlist, park list, dispose checklist, epistemic limits.
3. ! Dispose path MUST point at **#1396 decision log** (or interim parent-tracker comment until shipped) **and** optional **plan-sequence** for P1 — brief alone is not durable (#2741).
4. ⊗ End the skill after publishing the brief without stating the dispose checklist.
5. ⊗ Write SCM labels, run mirror apply, `task triage:accept`, or `task scope:promote|activate|complete` as part of this pass.

## Output surfaces

- ? Issue comment on the process/tracker issue (dogfood shape on #3198 pilot)
- ? Operator chat paste for immediate dispose
- ⊗ Treating the brief as applied governance without operator dispose

## EXIT

! On complete: `deft-directive-portfolio-prioritize complete -- exiting skill.` Chain: operator dispose → #1396 / `task plan-sequence:*` · optional `deft-directive-triage` for buildable queue · never silent exit.

## Anti-Patterns

- ⊗ SCM label writes / mirror apply / Wave 3 triage comments from this skill
- ⊗ Replacing `triage:queue` for buildable work selection
- ⊗ Unattended auto-promote
- ⊗ Title-only supersession or closed-state lies
- ⊗ Rank interrupt/escalate safety work against pattern/RFC park lists

## References

- #3198 (this skill); parent #886; stance #3179; dispose #1396; A-side #1423 / #3197; post-promotion siblings #1419 / #1511
- Siblings: `deft-directive-triage`, `deft-directive-refinement`
- Template: [`references/priority-brief-template.md`](references/priority-brief-template.md)
